import { Aldrich, Exo } from "next/font/google";

export const aldrich = Aldrich({
    weight: "400",
    subsets: ["latin"],
  });

export const exo = Exo({
    weight: "400",
    subsets: ["latin"],
    
  });